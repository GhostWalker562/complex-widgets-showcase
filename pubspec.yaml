name: codelessly_interview
description: Simple painters and interaction demonstration.
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # default
  cupertino_icons: ^0.1.3

  # preferences
  hive: ^1.4.2
  hive_flutter: ^0.3.1
  shared_preferences: ^0.5.10

  # development
  flutter_screenutil: ^2.3.1
  google_fonts: ^1.1.0
  simple_animations: ^2.2.1

  # State Management
  get_it: ^4.0.4
  injectable: ^1.0.2
  flutter_bloc: ^6.0.5
  freezed_annotation: ^0.11.0+1
  json_serializable: ^3.4.1

  # ui
  auto_size_text: ^2.1.0

  # features

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^0.7.1
  build_runner:
  injectable_generator: ^1.0.3
  auto_route_generator: ^0.6.7
  freezed: ^0.11.6

flutter:
  uses-material-design: true
  # assets:
    # assets-generator-begin
    # assets-generator-end
  
  
  
  

  
  

  
  

  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
